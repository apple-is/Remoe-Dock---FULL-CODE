class AppConstants {
  static const String appName = 'Remote Dock';
  static const String devicesBox = 'devices';
  static const Duration statusRefreshInterval = Duration(seconds: 5);
  static const Duration pingTimeout = Duration(seconds: 2);
  static const int defaultWoLPort = 9;
  static const int defaultShutdownPort = 8080;
}
